# settings.py - initializing the global variable

def init():

  global livefd;
  livefd = {};

  global closedfd;
  closedfd = [];

  global clonedfd;
  clonedfd = {};

  global logging;
  logging = False;

  global debugging;
  debugging = False;

  global iddoc;
  iddoc = 1;
